import { ValidatorConfig } from "../../typings";
declare enum NL_CODES {
    "btwNumber" = "btwNumber"
}
declare const NLValidator: Record<NL_CODES, ValidatorConfig>;
export { NLValidator, NL_CODES };
