import { ValidatorConfig } from "../../typings";
declare enum SE_CODES {
    CorporateIdentityNumber = "CorporateIdentityNumber"
}
declare const SEValidator: Record<SE_CODES, ValidatorConfig>;
export { SEValidator, SE_CODES };
